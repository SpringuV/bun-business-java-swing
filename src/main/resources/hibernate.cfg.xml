<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC "-//Hibernate/Hibernate Configuration DTD 3.0//EN" "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
	<session-factory name="session1">
		<property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>
		<property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
		<property name="hibernate.connection.url">
			jdbc:mysql://localhost:3306/business</property>
		<property name="hibernate.connection.username">root</property>
		<property name="hibernate.connection.password">hocmysql1234</property>
		<property name="hibernate.show_sql">true</property>
		<!-- JDBC connection pool settings -->
		<property name="hibernate.connection.pool_size">10</property>

		<!-- Echo all executed SQL to stdout -->
		<property name="hibernate.show_sql">true</property>
		<property name="hibernate.format_sql">true</property>

		<!-- Drop and re-create the database schema on startup -->
		<!-- Chọn một trong các option sau: -->
		<!-- create: tạo mới table mỗi lần chạy (xóa data cũ) -->
		<!-- create-drop: tạo khi start, xóa khi stop -->
		<!-- update: cập nhật schema nhưng giữ data (recommended cho
		development) -->
		<!-- validate: chỉ kiểm tra schema, không thay đổi gì -->
		<!-- none: không làm gì cả -->
		<property name="hibernate.hbm2ddl.auto">update</property>

		<!-- Enable Hibernate's automatic session context management -->
		<property name="hibernate.current_session_context_class">thread</property>

		<!-- Disable the second-level cache -->
		<property name="hibernate.cache.use_second_level_cache">false</property>

		<!-- HQL query factory -->
		<property name="hibernate.query.factory_class">
			org.hibernate.hql.internal.classic.ClassicQueryTranslatorFactory</property>
		<!-- Timezone configuration -->
		<property name="hibernate.jdbc.time_zone">Asia/Ho_Chi_Minh</property>

		<!-- ============================================ -->
		<!-- MAPPING CLASSES - Đăng ký các entity classes -->
		<!-- ============================================ -->
		<mapping class="model.Customer" />
		<mapping class="model.Food" />
		<mapping class="model.GuestTable" />
		<mapping class="model.Ingredient" />
		<mapping class="model.InventoryTransaction" />
		<mapping class="model.Invoice" />
		<mapping class="model.Orders" />
		<mapping class="model.OrderItem" />
		<mapping class="model.User" />
		<mapping class="model.WareHouse" />
	</session-factory>
</hibernate-configuration>